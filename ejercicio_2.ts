// Sumar todos los elementos de un arreglo de números
var numeros = [1, 2, 3, 4, 5, 6, 7, 8];
var suma = 0;

for(let i = 0; i < numeros.length; i++){
    suma += numeros[i];
}
console.log("la suma de total es: " + suma);